// TODO: Fix errors
/*
package betina.mendoza.Exercise9;

import javax.swing.*;
import java.awt.event.ActionEvent;
import java.awt.event.ActionListener;


public class PrimeNumber {
    private JPanel rootPanel;
    private JTextField txtInput;
    private JButton checkButton;

    public static void main(String[] args) {
        JFrame frame = new JFrame("PrimeNumber");
        frame.setContentPane(new PrimeNumber().rootPanel);
        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        frame.pack();
        frame.setVisible(true);
    }

    public PrimeNumber() {
        checkButton.addActionListener(new ActionListener() {
            @Override
            public void actionPerformed(ActionEvent e) {

                int primeNumber = Integer.parseInt(txtInput.getText());
                int n = 2;
                int ans = 0;
                while(n <= primeNumber / 2){
                    if(primeNumber % n == 0){
                        ans = 1;
                    }
                    n++;
                }
                if(ans == 1){
                    JOptionPane.showMessageDialog(null, "Number is not prime!");
                }
                else{
                    JOptionPane.showMessageDialog(null, "Number is prime!");
                }
            }
        });
    }
}
*/
