// TODO: Fix errors
/*
package angelica.olan;


public class Ex6 {

    String vendor;
    String item;
    int quantity;
    int currentTotal;
    int length;

//    public static void main(String [] args) {
      //  HashMap<String, String> hm = new HashMap<>();
       // hm.put("VENDOR", "Unilever");
      //  hm.put("QUANTITY", "9")
      //  System.out.println(hm.get("FIRST_NAME"));
       // System.out.println(hm.get("QUANTITY"));
       // hm.put("ITEM", "Soap");
       // System.out.println(hm.get("ITEM"));
public void setDate(String vendor) {this.vendor = vendor; }

    public float getTotalPOPrice() {
        float currentTotal = 0;
        for (int i = 0; < item.length; i++) {
            currentTotal += hm.get(items[i].getId()) *
        }
    }
    }
*/
